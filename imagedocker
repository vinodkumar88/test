node ("unix-ssh") {
    checkout scm

//    docker.withRegistry('https://hub.docker.com/repository/docker/vinodahri/get-started', 'dockerhubcert') {
//      docker.withRegistry('dockerhubcert') {
      docker.withDockerRegistry([credentialsId: 'dockerhubcert', url: 'https://hub.docker.com/repository/docker/vinodahri/get-started']) {

    def customImage = docker.build("app1t:v3")
//    def customImage = docker.build("app1t:v2" , "/root/workdir")

    customImage.push()
    customImage.inside {
//        sh 'make test'
        sh 'yum install -y ntp'
    }
    }
}
