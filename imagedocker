node ("unix-ssh") {
    checkout scm

      docker.withRegistry('https://hub.docker.com', 'dockerhubcert') {

    def customImage = docker.build("app1t:v3")
//    def customImage = docker.build("app1t:v2" , "/root/workdir")

    customImage.push()
    customImage.inside {
//        sh 'make test'
        sh 'yum install -y ntp'
    }
    }
}
